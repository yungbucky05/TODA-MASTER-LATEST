<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Main app theme (no action bar) -->
    <style name="Base.Theme.TODA" parent="android:Theme.Material.NoActionBar">
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/moto_splash</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.TODA</item>
    </style>

</resources>
